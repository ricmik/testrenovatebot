apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-secrets-operator
  namespace: argocd
spec:
  source:
    # renovate: registryUrl=https://charts.external-secrets.io chart=external-secrets
    targetRevision: "2.5.0" # renovate: depName=external-secrets/external-secrets extractVersion=^helm-chart-(?<version>.+)$
